{
  "name": "simple-db-viewer",
  "version": "1.0.2",
  "description": "Simple database viewer for PostgreSQL, MySQL, SQLite, and MongoDB (optional helper, not part of DB container runtime)",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prestart": "node -e \"const fs=require('fs');if(!fs.existsSync('node_modules')){console.log('node_modules not found. Running npm ci...');process.exit(1)}\" || npm ci",
    "start": "node server.js --host 0.0.0.0",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "4.18.2",
    "pg": "^8.11.3",
    "mysql2": "^3.6.3",
    "sqlite3": "^5.1.6",
    "mongodb": "^6.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}