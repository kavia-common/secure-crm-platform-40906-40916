# Placeholder env file to satisfy wildcard patterns (e.g., `*.env`) executed by external orchestration.
# This file contains no secrets and can remain committed safely.
# If you need real configuration, copy .env.example to .env and set values explicitly.
